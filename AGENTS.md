# ReEcho agent entry

Before any work, read these files in order:

1. `shared/AI_ONBOARDING.md`
2. `shared/CODEBASE_MAP.md` and its task-routing row; do not scan the whole repository by default
3. `shared/PROJECT_STATE.md`
4. `shared/PROJECT_RULES.md`
5. `shared/WORKFLOW.md` section 1
6. `shared/PLANNER_EXCHANGE.md`
7. Your role rules: `shared/EXECUTOR_RULES.md` by default, or `shared/PLANNER_RULES.md` when explicitly planning/reviewing/closing work
8. The assigned `plans/<id>-*.md` and only the relevant UE/DEBUG sections of `shared/LESSONS.md`

For code retrieval, start from the task-routing table in CODEBASE_MAP.md. Read the public header and paired implementation first, and expand the search only when that route is insufficient.

The authoritative project workflow lives under `shared/`. Root-level workflow documents are the imported blueprint source and are not project state.


