# Egao Party

| 项目 | 内容 |
| --- | --- |
| 显示名 | **Egao Party** |
| 分支名 | `Egao-Party`（Git 分支名不支持空格，故用连字符） |
| 作者 | **King of Party** |
| 基线 | `main` @ `05195466` |
| 性质 | 彩蛋版本 · 独立玩法模式分支 |

---

## 一、合入禁令（最高优先级）

本分支是**独立彩蛋版本**，**永远不得合入 `main`**。

明确禁止以下操作：

- 将 `Egao-Party` merge 进 `main`
- 向 `main` 发起包含本分支任何提交的 Pull Request
- 对 `main` 执行源自本分支的 rebase / cherry-pick / fast-forward
- 以本分支为基准重建 `main`

**分支关系**：本分支与 `main` 长期并行存在，只允许从 `main` **单向** 同步到本分支（可选），反向一律禁止。

**如果本分支的某个成果需要正式化**：请在一个全新的工作分支上重新实现并提交，走正常的 main 合入流程。不要通过合并本分支来实现——彩蛋版本可能包含为娱乐性而破坏平衡的改动，混入正式版本会造成难以追溯的问题。

---

## 二、模式规则

Egao Party 是一个**娱乐向彩蛋模式**：前期轻松割草，后期数值爆炸，靠夸张的卡牌堆叠制造混乱快感。
它直接改写正式数值表，不走难度倍率系统。

### 2.1 角色

四名角色基础属性**统一**为：

| 生命值 | 物理攻击力 | 元素攻击力 |
| --- | --- | --- |
| 66 | 16 | 16 |

（`Content/Data/characters.csv`，含描述文本同步更新）

### 2.2 怪物与投放

来源：`彩蛋关卡.xlsx` →「怪物属性和投放成长表」的**怪物分战斗数值表**与**波次投放表**。

- **怪物血量**（按战斗编号 1-8，三种怪一致）：`1 / 1 / 5 / 10 / 100 / 500 / 1000 / 2000`
- **怪物攻击力**：全程 `1`
- **攻击间隔**：史莱姆 1.3s、兔子 2s、狐狸 4s（沿用原值）
- **刷怪数量**：第 3 关起暴涨，Boss 关每波 `130 近战 / 140 远程 / 190 精英`

### 2.3 Boss（羊）

| 阶段 | 生命值 |
| --- | --- |
| 一阶段 | 1000000 |
| 二阶段 | 500000 |
| 三阶段 | 2000000 |

实现要点：一阶段取 `enemies.csv` 的 `M_SHEEP.MaxHealth`，二阶段取 `boss_phases.csv` 的
`PhaseMaxHealth`；三阶段由运行时按 `(一阶段 + 二阶段) × PreviousPhasesHealthMultiplier`
算出，因此该倍率固定为 **4/3**（正式版本为 5.0），一/二阶段改动后三阶段会自动跟随，
无需单独调整倍率。

### 2.4 卡牌

- **彩蛋卡刷新概率**：`1% → 10%`
- **取消独特性**：【彩蛋卡牌】与【样样都通】均可**重复选择**，拥有后仍会出现在卡池与投影中
- **连锁赠送**：玩家获得任意一张卡牌时，额外随机赠送 **1-5 张【样样都通】**

> 赠送采用直接入袋，不经过效果链，因此赠送的样样都通**不会再次触发赠送**（不会递归），
> 也不会触发它自身的 OnGrant。

---

## 三、维护约定

- **作者署名**：本分支的提交统一使用 `King of Party`。
- **不要在本分支上做正式修复**：正式 bug 修复与功能开发一律走 `main`，避免本分支被误当作可信来源。
- **提交信息**：建议以 `[EGAO PARTY]` 前缀开头，便于一眼区分来源。
- **资产放置**：本模式专属内容建议集中在独立目录，避免与正式资源混淆。
- **分支保护**：如有仓库管理权限，建议在 GitHub 上为本分支开启分支保护规则，从平台层面阻止误合入 `main`（见下节）。

---

## 四、如何在 GitHub 上锁死误合入

文档声明只能防君子，真正的保险是平台级保护。若你拥有仓库管理权限，建议：

1. 进入仓库 **Settings → Branches → Add branch ruleset**（或 Add rule）。
2. 分支名匹配填 `main`。
3. 勾选 **Require a pull request before merging**，并启用 **Require review from Code Owners**（可选）。
4. 更彻底的做法：在 `main` 的规则中启用 **Block deletions / Restrict who can push**，把推送权限限制给可信成员。
5. 若使用 Ruleset，还可设置 **Restrict creations**，进一步降低误操作风险。

完成上述配置后，任何人想改动 `main` 都必须经过 PR 与评审，从而断绝"误把彩蛋分支合进去"的可能。
