# `MOD-<Name>`：`<RuntimeModule>`

## 模块状态

- 当前状态：当前 `main` / 候选（候选文档不得进入远端当前架构索引）。
- Runtime Module：
- Build 文件：
- 主要目录：
- 相关 Plan：

## 存在原因

说明为什么需要这个模块，以及不拆分会产生什么耦合。

## 职责与排除项

### 负责

### 不负责

## 权威状态

列出模块独占写入的状态、生命周期和恢复方式。

## 输入、输出与公共契约

### 输入

### 输出

### 稳定公共类型/API

## 依赖方向

列出允许和禁止的模块依赖，说明原因。

## 运行时流程

描述从入口到结果发布的完整调用/数据流。

## 内部组成

按组件、普通 C++ 对象、Subsystem、Actor 适配器等职责拆解；不要把“组件化”本身当目的。

## 代码位置与阅读路线

| 目的 | Public 首读 | Private 实现 | 相关数据/资产 |
|---|---|---|---|

## 扩展方式

说明新增行为、数据类型、表现消费者或后端时应在哪个注册点扩展。

## 验证与测试

列出模块单元测试、跨模块集成测试、构建命令和需要用户人工判断的部分。

## 不变量与常见错误

记录模块级硬边界；可复用且有证据的具体事故另写入 `shared/LESSONS.md`。
